<?xml version="1.0"?>
<tomee>
    <Resource id="jdbc/orbiDB" type="DataSource">
        JdbcDriver  org.postgresql.Driver
        JdbcUrl     jdbc:postgresql://localhost/orbi
        UserName    orbi
        Password    orbi
    </Resource>

    <Resource id="SDFMail" type="javax.mail.Session">
        mail.smtp.host=smtp.gmail.com
        mail.smtp.port=587
        mail.smtp.starttls.enable=true
        mail.smtp.auth=true
        mail.smtp.user=${smtp.user}
        password=${smtp.password}
    </Resource>

</tomee>